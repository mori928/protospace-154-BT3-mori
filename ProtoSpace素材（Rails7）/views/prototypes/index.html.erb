<main class="main">
  <div class="inner">
    <div class="greeting">
    <% if user_signed_in? %>  
        こんにちは、
        <%= current_user.username %> さん
        <%# <%= link_to "#{current_user.username}さん", マイページへのroot_path, class: :greeting__link %>
      </div>
    <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
    <% end %>
  </div>
</main>
